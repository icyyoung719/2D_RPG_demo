# 概览

本项目是一个小型的 2D RPG 演示工程，目标是提供：

- 基于 SFML 的渲染循环与窗口管理
- 简单的实体与动画系统（见 `src/Animation.cpp`, `src/Player.cpp`）
- 地图加载器与瓦片集支持（使用 JSON 格式的关卡/tileset，位于 `assests/world/` 与 `assests/maps/`）

该工程适合用于学习游戏循环、碰撞与关卡加载，也可作为个人项目的起点。
